{
  "scripts": {
    "watch": "sass --watch scss/menu.scss:css/menu.css",
    "prefix": "postcss ./css/page/menu.css ./css/main.css --use autoprefixer -d ./css/prefixed/"
  },
  "dependencies": {
    "sass": "^1.48.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.2",
    "postcss-cli": "^9.1.0",
    "browserslist": "last 4 versions"
  }
}
